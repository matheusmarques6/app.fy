# Android Shell

This directory is a placeholder. The actual Android project is generated by:

```bash
npx cap add android
```

Do not manually create files here. Capacitor manages the native Android project.
