[build]
builder = "nixpacks"
installCommand = "npm install -g pnpm@9 && cd ../.. && pnpm install --frozen-lockfile"
buildCommand = "cd ../.. && pnpm --filter @appfy/shared build && cd apps/api && pnpm db:generate && pnpm build"

[deploy]
startCommand = "pnpm db:migrate:prod && node dist/main.js"
healthcheckPath = "/v1/health/live"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
