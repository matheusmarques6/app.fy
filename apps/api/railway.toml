[build]
builder = "nixpacks"

[deploy]
startCommand = "pnpm --filter @appfy/api prisma:migrate:deploy && node dist/main.js"
healthcheckPath = "/v1/health/live"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
